# 빠른 시작 가이드

## 🚀 5분 안에 시작하기

### 1. 패키지 설치
```bash
cd mapotoday_bot
pip install -r requirements.txt
```

### 2. .env 파일 생성
`mapotoday_bot` 폴더에 `.env` 파일을 만들고 아래 내용을 입력하세요:

```env
TELEGRAM_BOT_TOKEN=8225463837:AAEAHLITWzQ2Zvr5sLsXEt8qx6KnF2O6_gA
NAVER_CLIENT_ID=여기에_네이버_Client_ID
NAVER_CLIENT_SECRET=여기에_네이버_Client_Secret
CHAT_ID=여기에_텔레그램_채팅_ID
```

### 3. 네이버 API 키 발급 (2분)
1. https://developers.naver.com/ 접속
2. 로그인 후 "Application" → "애플리케이션 등록"
3. 이름 입력, "검색" API 선택
4. Client ID와 Client Secret 복사해서 `.env`에 입력

### 4. 텔레그램 채팅 ID 확인 (1분)
1. 텔레그램에서 `@userinfobot` 검색
2. 봇에게 `/start` 전송
3. 응답에서 "Id" 값 복사해서 `.env`의 `CHAT_ID`에 입력

### 5. 실행
```bash
cd mapotoday_bot
python bot.py
```

또는 이미 `mapotoday_bot` 폴더 안에 있다면:
```bash
python bot.py
```

완료! 이제 5분마다 '마포' 관련 최신 뉴스를 받을 수 있습니다.

---

## 📱 텔레그램 채팅 ID 확인 방법 (상세)

### 개인 채팅 (자신에게 보내기)
1. 텔레그램 앱 열기
2. 검색창에 `@userinfobot` 입력
3. 봇 선택 후 아무 메시지나 전송 (예: `/start`)
4. 봇이 보내는 메시지에서 **"Id"** 값 확인
   - 예: `Id: 123456789` → `123456789` 사용

### 그룹 채팅
1. 그룹에 `@userinfobot` 추가
2. 봇에게 `/start` 전송
3. 봇이 응답한 그룹 ID 사용 (보통 음수: `-1001234567890`)

### 채널
1. 채널에 봇을 관리자로 추가
2. 채널 ID는 `-100`으로 시작하는 숫자
3. `@getidsbot` 같은 봇으로 확인 가능

---

## ❓ 자주 묻는 질문

**Q: 봇이 실행은 되는데 메시지를 받지 못해요**
- A: `.env` 파일의 `CHAT_ID`가 올바른지 확인하세요. 개인 채팅의 경우 봇에게 먼저 메시지를 보내야 합니다.

**Q: "API 호출 오류"가 나와요**
- A: 네이버 API 인증 정보가 올바른지 확인하세요. 네이버 개발자 센터에서 애플리케이션이 정상 등록되었는지 확인하세요.

**Q: 중복 기사가 계속 와요**
- A: `processed_articles.json` 파일을 삭제하고 다시 시작하세요.

**Q: 검색 키워드를 바꾸고 싶어요**
- A: `bot.py` 파일의 `SEARCH_QUERY = '마포'` 부분을 원하는 키워드로 변경하세요.

**Q: 검색 간격을 바꾸고 싶어요**
- A: `bot.py` 파일의 `schedule.every(5).minutes.do(...)` 부분에서 `5`를 원하는 분으로 변경하세요.

---

## 🔧 고급 설정

### 백그라운드 실행 (서버에서 24시간 실행)
```bash
cd mapotoday_bot
nohup python bot.py > bot.log 2>&1 &
```

### 로그 확인
```bash
cd mapotoday_bot
tail -f bot.log
```

### 실행 중인 봇 중지
```bash
ps aux | grep bot.py  # 프로세스 ID 확인
kill [프로세스_ID]
```
